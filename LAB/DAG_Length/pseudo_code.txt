#include <bits/stdc++.h>
using namespace std;
函式main{
    宣告一整數陣列adj儲存最大及最小路徑
    宣告node, edge, source, goal為整數變數

    輸入節點數量和邊的數量
    輸入當前節點和目標節點

    for迴圈輸入node次{
        將數對無限大無限小放入adj中
    }
    將起始點數對初始化為0

    宣告begin, end, w為整數變數

    for迴圈輸入edge次{
        輸入起始點和目標點及邊長
        更新起點到目標點的最短距離
        更新起點到目標點的最長距離
    }
    如果目標點的最小等於無限大輸出No path
    否則輸出最長距離及最短距離
}